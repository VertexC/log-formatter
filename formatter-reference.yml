# Elastic Search Input Output Only 
output:
  target: "elasticsearch"
  elasticsearch:
    host: "<host>"
    index: "<index>"
input:
  target: "elasticsearch"
  elasticsearch:
    host: "<host>"
    quries:
      - index: "<index>"
        body: '{
          "from":0,
          "size":100,
          "query":{
            "match": {
              "match_all": {}
            }
          }
        }'
        formatter: "mongodb"
      